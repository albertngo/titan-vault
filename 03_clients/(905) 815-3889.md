---
type: client
# Note floor: staff may see this note; admin-grade bullets carry a trailing #admin
# (see CONVENTIONS: Visibility). Set admin only when the whole entity is admin-sourced.
visibility: staff
status: prospect
last_activity: 2026-09-11
# Source-system IDs — the join keys. Names are display; IDs are identity.
# Agents match on these BEFORE name, so a rename in GHL never creates a duplicate note.
ghl_contact_id: VKuScz6iKSgelC3uHNnp
ghl_conversation_ids: []
# Operational, not identity: who owns this record in GHL right now (name + id).
ghl_assigned_to:
ghl_assigned_to_id:
---

# (905) 815-3889

**Contact:** +19058153889
**Address:** Oakville
**Source:** Direct (unattributed inbound call)

## Context
<!-- human-owned: who they are, what they want, quirks -->

## Opportunities
None yet.

## Log
- 2026-09-11 — new caller from GHL daily ingest, top-level `needs_attention` (priority high): inbound call yesterday afternoon, no callback, no SMS, and no tag applied since — a plain missed callback, not routine call-queue noise. Checked the vault by contact ID and name first — no existing match.
