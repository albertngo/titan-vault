---
type: client
visibility: staff
status: prospect
last_activity: 2026-09-26
# Source-system IDs — the join keys. Names are display; IDs are identity.
# Agents match on these BEFORE name, so a rename in GHL never creates a duplicate note.
ghl_contact_id: BgwPscQTeFeLe4stlifY
ghl_conversation_ids: [WQds853MGLXD4pxXIFgX]
# Operational, not identity: who owns this record in GHL right now (name + id).
# Used to decide whom the next task gets assigned to. Absent = unassigned in GHL.
ghl_assigned_to:
ghl_assigned_to_id:
---

# Richard Harris

**Contact:** —
**Address:** —
**Source:** GHL lead — Meta ad, June, on a generic nurture drip until re-engaged

## Context
<!-- human-owned: who they are, what they want, quirks -->

## Opportunities
None yet.

## Log
- 2026-08-01 — created from GHL daily ingest `needs_attention`: warm Meta-ad lead from June, on a generic nurture drip since. Replied "yes please and thank" to Albert's re-engagement text late 2026-07-31 — ready to talk specifics, still unread as of this run (~12h). Needs a real follow-up call today, not another template.
- 2026-09-22 — GHL daily ingest, top-level `needs_attention` (item 2, priority high) + drift `abandonment_next`: the Warm-stage opportunity is now 95.9 days in `0b. Later Date (Warm)` (240% of the 40-day threshold, more than double the 80-day auto-abandon point) — still open, no `stale_lead` tag; same stuck-automation pattern as the Hot-stage cluster flagged today. No reply logged since the 2026-07-31 re-engagement text.
- 2026-09-26 — GHL daily ingest, top-level `needs_attention` + drift `stale_approaching` (high, 250% of the 40-day threshold; opportunity `bncGJOBODCXong8B2oMz`, contact `BgwPscQTeFeLe4stlifY`): the Warm-stage opportunity is now 99.9 days in `0b. Later Date (Warm)` — still open, no `stale_lead` tag, same stuck-automation pattern flagged 2026-09-22 (then 240% of threshold). No reply logged since the 2026-07-31 re-engagement text.
