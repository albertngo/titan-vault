---
type: client
# Note floor: staff may see this note; admin-grade bullets carry a trailing #admin
# (see CONVENTIONS: Visibility). Set admin only when the whole entity is admin-sourced.
visibility: staff
status: active
last_activity: 2026-08-20
# Source-system IDs — the join keys. Names are display; IDs are identity.
# Agents match on these BEFORE name, so a rename in GHL never creates a duplicate note.
ghl_contact_id: ygeNqFaUFfnveDoogUMr
ghl_conversation_ids: [ILhbGDtayfVWYYI5gLfF]
# Operational, not identity: who owns this record in GHL right now (name + id).
# Used to decide whom the next task gets assigned to. Absent = unassigned in GHL.
ghl_assigned_to:
ghl_assigned_to_id:
---

# Phuong Nguyen

**Contact:** phone on file in GHL (see `ghl_contact_id`)
**Address:** (not provided in today's ingest)
**Source:** GHL — pos-tagged store/material inquiry

## Context
<!-- human-owned: who they are, what they want, quirks -->

## Opportunities
None noted yet — pos-tagged inquiry, no linked opportunity in today's ingest.

## Log
- 2026-08-20 — created from GHL daily ingest `needs_attention`: inbound "Ya I can pay cash" on a pos-tagged conversation (`ILhbGDtayfVWYYI5gLfF`), sitting ~38h with Titan owing the reply — a ready-to-close cash sale waiting on our confirmation. Ranked #2 in today's GHL stragglers list; flagged in today's `needs_attention`.
